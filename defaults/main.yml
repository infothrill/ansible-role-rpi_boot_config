---
# defaults for rpi-boot-config

# Path to the Raspberry Pi boot configuration file
rpi_boot_config_file: /boot/config.txt

boot_config: {}
#  gpu_mem: 64

boot_config_lines: []
#  - "gpu_mem=64"

# wether to reboot if the config changed:
rpi_boot_config_reboot: true
